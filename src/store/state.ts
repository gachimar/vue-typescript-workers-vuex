import { RootState } from "./types"
import ExampleModuleState from './modules/state'
const state : RootState = {
    message: 'Oreja de vangoh',
    alertery: ExampleModuleState
}

export default state
